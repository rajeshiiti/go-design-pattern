The Bridge Design Pattern is a structural pattern that separates an abstraction 
from its implementation so that the two can evolve independently. 
It helps avoid a monolithic inheritance structure by using composition instead of inheritance.